const homeController = require('./home');
const authController = require('./auth');
const userController = require('./user');

module.exports = {
  homeController,
  authController,
  userController,
};
