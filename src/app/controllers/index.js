const homeController = require('./home');
const authController = require('./auth');
const authExtraController = require('./auth-extra');
const userController = require('./user');

module.exports = {
  homeController,
  authController,
  authExtraController,
  userController,
};
